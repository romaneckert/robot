node js project for hexapod controlling - use jeneric

# clean

gulp clean

# raspberry pi

reduce power comsumption

Disable HDMI

/usr/bin/tvservice -o

Enable HDMI

/usr/bin/tvservice -p

Add the line to /etc/rc.local to disable HDMI on boot.